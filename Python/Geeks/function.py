# the function is declared by the keyword 'def'


def x (a, b):  # Here x is example for name ..it may be x, name dive ,etc.....
    if a % b == 0:
        print ("a is divisable by b")
    elif b % a == 0:
        print ("b is divisable by a")
    else:
        print ("a is not divisable by b")
#program to check the above function

x (3, 21)
